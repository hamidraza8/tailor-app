import 'package:flutter_test/flutter_test.dart';

void main() {
  // Smoke test placeholder — full integration tests are in integration_test/
  test('placeholder', () {
    expect(1 + 1, equals(2));
  });
}
